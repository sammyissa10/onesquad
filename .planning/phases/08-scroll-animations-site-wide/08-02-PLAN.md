---
phase: 08-scroll-animations-site-wide
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/components/sections/PortfolioPreview.tsx
  - src/components/sections/Process.tsx
  - src/components/sections/Testimonials.tsx
  - src/components/sections/CTABanner.tsx
  - src/components/sections/ServicesHero.tsx
  - src/components/sections/DigitalMarketingGrid.tsx
  - src/components/sections/WebSolutionsGrid.tsx
  - src/app/services/page.tsx
autonomous: true

must_haves:
  truths:
    - "Homepage second half (PortfolioPreview, Process, Testimonials, CTABanner) uses GSAP scroll reveals, not Framer Motion"
    - "Services overview page (hero, DM grid, WS bento, CTA) uses GSAP scroll reveals, not Framer Motion"
    - "Process timeline steps stagger in alternating from left/right"
    - "Testimonial bento cards stagger with varied timing (not uniform)"
    - "Each section has a distinct entrance effect (no two identical)"
  artifacts:
    - path: "src/components/sections/PortfolioPreview.tsx"
      provides: "GSAP-animated portfolio preview"
      contains: "useScrollAnimation"
    - path: "src/components/sections/Process.tsx"
      provides: "GSAP-animated process timeline"
      contains: "useScrollAnimation"
    - path: "src/components/sections/Testimonials.tsx"
      provides: "GSAP-animated testimonials"
      contains: "useScrollAnimation"
    - path: "src/components/sections/CTABanner.tsx"
      provides: "GSAP-animated CTA"
      contains: "useScrollAnimation"
    - path: "src/components/sections/ServicesHero.tsx"
      provides: "GSAP-animated services hero"
      contains: "useScrollAnimation"
    - path: "src/components/sections/DigitalMarketingGrid.tsx"
      provides: "GSAP-animated DM grid"
      contains: "useScrollAnimation"
    - path: "src/components/sections/WebSolutionsGrid.tsx"
      provides: "GSAP-animated WS bento"
      contains: "useScrollAnimation"
  key_links:
    - from: "src/components/sections/Process.tsx"
      to: "src/lib/scrollAnimations.ts"
      via: "imports animation presets"
      pattern: "import.*from.*@/lib/scrollAnimations"
    - from: "src/components/sections/ServicesHero.tsx"
      to: "src/hooks/useScrollAnimation.ts"
      via: "hook usage"
      pattern: "useScrollAnimation"
---

<objective>
Convert the remaining homepage sections (PortfolioPreview, Process, Testimonials, CTABanner) and all Services overview page sections from Framer Motion to varied GSAP ScrollTrigger animations.

Purpose: Complete homepage scroll animation coverage and extend to the Services overview page. Each section gets a distinct animation personality matching its content type.

Output: 8 component files converted from Framer Motion to GSAP ScrollTrigger.
</objective>

<execution_context>
@C:/Users/sammy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sammy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-scroll-animations-site-wide/08-01-SUMMARY.md
@src/lib/scrollAnimations.ts
@src/hooks/useScrollAnimation.ts
@src/components/sections/PortfolioPreview.tsx
@src/components/sections/Process.tsx
@src/components/sections/Testimonials.tsx
@src/components/sections/CTABanner.tsx
@src/components/sections/ServicesHero.tsx
@src/components/sections/DigitalMarketingGrid.tsx
@src/components/sections/WebSolutionsGrid.tsx
@src/app/services/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert homepage second half sections to GSAP</name>
  <files>
    src/components/sections/PortfolioPreview.tsx
    src/components/sections/Process.tsx
    src/components/sections/Testimonials.tsx
    src/components/sections/CTABanner.tsx
  </files>
  <action>
Convert four homepage sections from Framer Motion to GSAP ScrollTrigger, each with a DISTINCT animation personality.

**PortfolioPreview.tsx** — Peach section, template grid cards
- Current: Framer Motion stagger(0.08) + scaleIn variants, useInView
- Target animation: **Clip-path reveal for heading + staggered fadeUp for cards**
- Remove framer-motion imports, motion components, useInView, containerVariants/itemVariants
- Section heading: clipReveal('bottom') — reveals from bottom to top for dramatic impact
- Grid cards: staggered fadeUp with 0.08s each, trigger on grid container start at TRIGGERS.late
- CTA: simple fadeUp with 0.3s delay after cards
- TemplateGridCard components are external — do NOT modify them. Wrap them in `<div className="portfolio-card" data-animate>` for GSAP targeting
- Note: The `scaleIn` and `stagger` imports from `@/lib/animations` must be removed

**Process.tsx** — Navy section, 4 process steps in alternating timeline
- Current: Framer Motion stagger(0.15) + fadeIn variants, useInView
- Target animation: **Alternating slide-in** — steps alternate sliding from left/right to match the alternating layout
- Remove framer-motion imports and motion components
- Section heading: fadeUp with early trigger
- Process steps: Loop through `.process-step` elements. Even-indexed (isLeft) steps use slideFromLeft, odd-indexed use slideFromRight. Each gets its own ScrollTrigger with the step element as trigger. Stagger naturally via scroll position (each step triggers independently as user scrolls)
- Icon nodes on the timeline: scale from 0 with a slight delay after the step content: `gsap.from('.process-icon', { opacity: 0, scale: 0, duration: 0.4, stagger: 0.15, scrollTrigger: { trigger: '.process-timeline', start: TRIGGERS.standard } })`

**Testimonials.tsx** — Peach section, bento-grid testimonial cards
- Current: Framer Motion stagger(0.15) + scaleIn, useInView
- Target animation: **Staggered scale with varied timing** — each card scales in with different delays creating a "popcorn" effect
- Remove framer-motion imports, motion components
- Section heading: fadeUp with early trigger
- Testimonial cards: Each gets individual ScrollTrigger. First card (wide, coral): scaleReveal with no delay. Second card (narrow, navy): scaleReveal with 0.15s delay. Third card (full-width, white): fadeUp with 0.3s delay (different effect for the widest card). This variation prevents uniform feel.
- Keep the hover:scale-[1.02] as existing Tailwind `transition-transform duration-300` (already in className)

**CTABanner.tsx** — Navy section, centered text with CTAs
- Current: Framer Motion stagger(0.15) + fadeIn, useInView
- Target animation: **Dramatic fadeUp with increasing delay** — headline appears first, subtext slightly after, buttons last, trust badges last
- Remove framer-motion imports, motion components, useInView
- Heading: `gsap.from('.cta-heading', { ...fadeUp({ y: 40, duration: 0.8 }), scrollTrigger: { trigger: '.cta-content', start: TRIGGERS.standard } })`
- Subtext: fadeUp with 0.15s delay
- Buttons: fadeUp with 0.3s delay
- Trust badges: fadeUp with 0.45s delay, slight scale (0.95 to 1)

For all four files:
- Import `useScrollAnimation` from `@/hooks/useScrollAnimation`
- Import needed presets from `@/lib/scrollAnimations`
- Remove Framer Motion animation imports (stagger, fadeIn, scaleIn from @/lib/animations AND motion/useInView from framer-motion)
- Replace `<motion.div>` with `<div>` + CSS targeting classes
- Add `data-animate` to elements that start hidden
- Keep all content, copy, links, layout, and Tailwind unchanged
- Keep HomeFAQ.tsx as-is — its AnimatePresence accordion open/close is interactive animation, not scroll reveal
  </action>
  <verify>
Run `npm run build` — no TypeScript errors. No framer-motion imports in PortfolioPreview.tsx, Process.tsx, Testimonials.tsx, CTABanner.tsx. Scroll through full homepage — each section has distinct entrance animation.
  </verify>
  <done>PortfolioPreview uses clip-path reveal + staggered fadeUp. Process uses alternating slide-in. Testimonials use varied-timing scale reveals. CTABanner uses cascading fadeUp with increasing delays. HomeFAQ accordion untouched (interactive animation).</done>
</task>

<task type="auto">
  <name>Task 2: Convert Services overview page sections to GSAP</name>
  <files>
    src/components/sections/ServicesHero.tsx
    src/components/sections/DigitalMarketingGrid.tsx
    src/components/sections/WebSolutionsGrid.tsx
    src/app/services/page.tsx
  </files>
  <action>
Convert the Services overview page from Framer Motion to GSAP ScrollTrigger. The services page personality is bold and functional — animations should feel confident, not playful.

**ServicesHero.tsx** — Navy section, large headline with CTAs
- Current: Framer Motion stagger(0.12) + fadeIn, useInView
- Target animation: **Bold fadeUp cascade** — headline sweeps up with power3 easing (heavier, more dramatic), subtext and buttons follow
- Remove framer-motion imports, motion components, useInView
- Use useScrollAnimation with scope on section
- Headline: `gsap.from('.services-hero-headline', { opacity: 0, y: 60, duration: 0.9, ease: 'power3.out', scrollTrigger: { trigger: '.services-hero-content', start: TRIGGERS.hero } })`
- Subtext: fadeUp with 0.15s delay, standard easing
- Buttons row: fadeUp with 0.3s delay
- Add data-animate to animated elements

**DigitalMarketingGrid.tsx** — White section, 3-col card grid with glow hover
- Current: Framer Motion stagger(0.08) + fadeIn, useInView
- Target animation: **Staggered fadeUp from center** — cards emanate from center of grid
- Remove framer-motion imports, motion components, useInView
- Category header: fadeUp with early trigger
- Cards: `gsap.from('.dm-card', { ...fadeUp({ y: 40 }), stagger: { each: 0.1, from: 'center' }, scrollTrigger: { trigger: '.dm-grid', start: TRIGGERS.standard } })`
- Keep hover effects as-is (CSS group-hover classes)

**WebSolutionsGrid.tsx** — Navy section, asymmetric bento with lift+shadow hover
- Current: Framer Motion stagger(0.1) + fadeIn, useInView, motion.div whileHover={{ y: -6 }}
- Target animation: **Scale reveal with bento rhythm** — hero card (2-col span) reveals via scaleReveal, smaller cards stagger with fadeUp
- Remove framer-motion imports, motion components, useInView
- Category header: fadeUp with early trigger
- Hero card (index 0, col-span-2): `gsap.from('.ws-hero-card', { ...scaleReveal({ duration: 0.7 }), scrollTrigger: { trigger: '.ws-grid', start: TRIGGERS.standard } })`
- Remaining cards: staggered fadeUp with 0.12s stagger, starting 0.2s after hero card
- Replace motion.div whileHover={{ y: -6 }} with Tailwind `hover:-translate-y-1.5 transition-transform duration-300`

**services/page.tsx** — Inline CTA section at bottom
- Current: Framer Motion motion.div with whileInView
- Target animation: **Simple fadeUp** with standard trigger
- Remove framer-motion import, motion component
- This is a small section — wrap in useScrollAnimation or use a simple approach:
  - Make the page component use useScrollAnimation hook
  - Apply fadeUp to the `.services-cta` container
- Keep MagneticButton, Link, Button components unchanged

For all four files:
- Import useScrollAnimation from @/hooks/useScrollAnimation
- Import needed presets from @/lib/scrollAnimations
- Remove all Framer Motion imports (stagger, fadeIn from @/lib/animations AND motion, useInView from framer-motion)
- Replace motion.div with div + targeting classes
- Add data-animate to elements starting hidden
- Keep all content, layout, hover patterns unchanged
  </action>
  <verify>
Run `npm run build` — no TypeScript errors. No framer-motion imports in ServicesHero.tsx, DigitalMarketingGrid.tsx, WebSolutionsGrid.tsx. Visit /services — hero animates on load, DM grid stagers from center, WS bento uses scale reveal for hero card, CTA fades up.
  </verify>
  <done>Services overview page fully converted to GSAP ScrollTrigger. Hero uses dramatic fadeUp, DM grid staggers from center, WS bento uses scale reveal for hero card + staggered fadeUp for smaller cards, CTA uses simple fadeUp. All hover effects preserved.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. Full homepage scroll — all 9 sections animate distinctly (Hero: stagger, Features: scale, ServicesPreview: fadeUp, Comparison: slide-in, PortfolioPreview: clip+fadeUp, Process: alternating slide, Testimonials: varied scale, CTABanner: cascade, HomeFAQ: unchanged interactive)
3. Services overview page — 4 sections animate with different effects
4. No framer-motion imports in any converted section component
5. HomeFAQ.tsx still uses AnimatePresence for accordion (interactive, not scroll)
6. All animations respect prefers-reduced-motion
</verification>

<success_criteria>
- 8 files modified (4 homepage sections + 4 services sections)
- Each section has a distinct scroll animation effect
- Zero Framer Motion imports in converted files
- Homepage and Services overview page fully covered
- Interactive animations (HomeFAQ accordion) left intact
</success_criteria>

<output>
After completion, create `.planning/phases/08-scroll-animations-site-wide/08-02-SUMMARY.md`
</output>
